PARA MO START:
Just do this:

go to shareth-backend
create env file and paste the mongodb url

npm install mongodb

new terminal, cd BlockChain-dApp

npm install

npm run dev

Take note: dapat naa mo sa ShareEthNotes Folder
